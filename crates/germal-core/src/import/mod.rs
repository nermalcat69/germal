//! 把别处的请求描述读进 [`RequestDraft`]，与 [`crate::codegen`]（出）方向相反。
//!
//! 两边共用一条判据：**只搬「这条请求是什么」，不搬「怎么发」**。TLS 校验、
//! 重定向、超时这些是 Germal 的运行时设置，`codegen` 刻意不生成它们，
//! 这里也刻意不读它们——读进来只会和用户在设置里的选择打架。

pub mod curl;
